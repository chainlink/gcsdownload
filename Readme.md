# GCS Download

Allows for authenticated download from a GCS bucket

```
$ gcsdownload -h
  -b string
        Bucket name
  -f string
        filename to call object
  -k string
        GCP key.json file
  -o string
        object to download
```
